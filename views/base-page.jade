-	var phrases = require('./texts/phrases.js');
-	var getPhrase = function (){var idx = Math.floor(Math.random()*phrases.length); return phrases[idx]}

mixin HEAD_TITLE
	.container-fluid
		.container
			div.phrase.text-center
				span Phrase for today : &#009;
					span!= getPhrase()
			.title
				h3 Logo Title
mixin BACKGROUND_IMAGE
	.container-fluid.page-bg(style="")

mixin PHRASE
	div.phrase.text-center
		span Phrase for today : &#009;
			span!= getPhrase()

mixin CONTAINER_MD_8_CENTER
	.container-fluid
		.row
			.col-sm-12.col-md-8.col-md-offset-2
				block

mixin CONTAINER_MD_6_CENTER
	.container-fluid
		.row
			.col-sm-12.col-md-6.col-md-offset-3
				block

mixin CONTAINER_MD_4_CENTER
	.container-fluid
		.row
			.col-sm-12.col-md-4.col-md-offset-4
				block
